version: '3.8'

services:
  piston:
    image: ghcr.io/engineer-man/piston
    container_name: questboard-piston
    restart: unless-stopped
    ports:
      - "2000:2000"
    volumes:
      - piston-packages:/piston/packages
    tmpfs:
      - /tmp:exec

volumes:
  piston-packages:
